import React from "react";
import './icon.css'

const Icon = (title) => {
    return (
        <a href="/" className="nav-link">
        <i className="fa-solid fa-house icon"></i>
        {{}}
      </a>
    );
};

export default Icon;